package cmds

import (
	"testing"
)

func TestDescriptionCmd(t *testing.T) {
	// start a new cli app
	// call this func
	// =============
	// test to make sure app has
	// loaded this particular function
}

func TestDescriptionInfo(t *testing.T) {
	// set argument one in command line
	// app to something like "ruby"
	// call this function
	// =============
	// test to make sure it only prints the description
}
